<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <%= link_to 'Home', root_path, :class => "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
            <li class="nav-item">
                <a class="nav-link" href="/scoring">Performances</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/spider">Spider</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/crawltemp">W3C</a>
            </li>
        <% end %>
        </ul>
        <ul class="navbar-nav">
            <% if user_signed_in? %>
                <li class="nav-item">
                    <%= link_to "Sign out", destroy_user_session_path, :class => "nav-link"%>
                </li>
            <% else %>
                <li class="nav-item">
                    <%= link_to 'Connection', new_user_session_path, :class => "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Register', new_user_registration_path, :class => "nav-link" %>
                </li>
            <% end %>
        </ul>
    </div>
</nav>